const SEARCH_CARD = 'SEARCH_CARD';

const cardReducer = {state, action} => {
  switch (action.type) {
    case SEARCH_CARD:
      return state;
    default:
      return state;
  }
};

export default cardReducer;
